name: xtyle
description: Xtyle provides mutiligual-styled Text and TextEditingController. You can use diffrenet fontFamily for each languages.
version: 0.1.1
homepage: https://github.com/TykanN/xtyle

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: